import initialState from 'esfera/store/initialState';
import {} from '../types';

const AddBillsReducer = (state = initialState.auth, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
export default AddBillsReducer;
