import initialState from 'esfera/store/initialState';
import {} from '../types';

const BillsReducer = (state = initialState.auth, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
export default BillsReducer;
