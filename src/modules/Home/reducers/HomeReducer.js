import initialState from "../../../store/initialState";
import {} from "../types";

const HomeReducer = (state = initialState.auth, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
export default HomeReducer;
