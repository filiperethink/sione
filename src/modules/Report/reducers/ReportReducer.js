import initialState from 'esfera/store/initialState';
import {} from '../types';

const ReportReducer = (state = initialState.auth, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
export default ReportReducer;
